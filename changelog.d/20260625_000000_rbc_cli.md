---
bump: minor
---

### Added
- Added `rbc` for browser automation through CLI commands without MCP, including browser-pool names, legacy project-first calls, direct CDP/shared-extension targets, JSON output, screenshots, tab control, and JSONL traces.
- Added `rbc <browser> pw` for running Playwright scripts against CDP-backed Chromium/Edge targets and a Playwright-compatible subset against shared Edge extension targets.
